from math import sqrt,floor
num = int(input('Digite um número:'))
raiz = sqrt(num)
print('A raiz de {} é igual a {:.2f}'.format(num,raiz))

#Neste caso com a importação de funções dentro da bibliotéca podemos digitar
# diretamente a função sem colocar o math.